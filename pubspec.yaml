name: ratfpt
description: FXTP Remote Access Tool – Pure Flutter
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.12.0
  device_info_plus: ^10.1.2
  permission_handler: ^11.3.1
  file_picker: ^8.1.0
  share_plus: ^10.1.1
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
